# Pinned build dependencies for the MkDocs documentation site.
# Used by .github/workflows/docs.yml. Bump versions deliberately.
mkdocs-material==9.7.6
mkdocs-static-i18n==1.3.1
# Pinned to 1.2.2, not the current 1.2.3: as of this release, 1.2.3 on PyPI
# pulls in an unrelated third-party package ("properdocs") as a runtime
# dependency and prints an unsolicited migration notice for it on every
# build. 1.2.2 has no such dependency. Re-check before bumping past 1.2.3.
mkdocs-redirects==1.2.2
